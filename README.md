# Mailinator Docs   

The new Mailinator API docs are built from an OpenAPI specification file. This file is the source of truth for the new API docs, so any new changes need to be made directly to the specification.